# Concurrent Hash Table Implementation

## How to Compile
Run `make` in the terminal.

## How to Run
Run `./chash`. The program reads `commands.txt` and writes results to `output.txt`.

## AI Citation
This implementation was generated with AI assistance.
